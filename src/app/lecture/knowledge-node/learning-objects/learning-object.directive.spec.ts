import { LearningObjectDirective } from './learning-object.directive';

describe('LearningObjectDirective', () => {
  it('should create an instance', () => {
    const directive = new LearningObjectDirective();
    expect(directive).toBeTruthy();
  });
});
