import {LearningObjectSummary} from './learning-object-summary';

describe('LearningObjectSummary', () => {
  it('should create an instance', () => {
    expect(new LearningObjectSummary({})).toBeTruthy();
  });
});
