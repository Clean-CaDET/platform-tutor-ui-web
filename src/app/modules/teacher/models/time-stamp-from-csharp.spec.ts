import { TimeStampFromCSharp } from './time-stamp-from-csharp';

describe('TimeStampFromCSharp', () => {
  it('should create an instance', () => {
    expect(new TimeStampFromCSharp()).toBeTruthy();
  });
});
