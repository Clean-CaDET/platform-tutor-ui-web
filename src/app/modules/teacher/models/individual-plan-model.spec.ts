import { IndividualPlanModel } from './individual-plan-model';

describe('IndividualPlanModel', () => {
  it('should create an instance', () => {
    expect(new IndividualPlanModel()).toBeTruthy();
  });
});
